<Window x:Class="InterviewCopilot.SessionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="My Interview Sessions"
        Height="540" Width="840"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        UseLayoutRounding="True" SnapsToDevicePixels="True">

    <Window.Resources>
        <Style x:Key="GhostBtn" TargetType="Button">
            <Setter Property="Background" Value="#14141C"/>
            <Setter Property="Foreground" Value="#E0E0E4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#28FFFFFF"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FocusVisualStyle" Value="{StaticResource KeyboardFocus}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#20202A"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#50FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerBtn" TargetType="Button">
            <Setter Property="Background" Value="#35171D"/>
            <Setter Property="Foreground" Value="#FF858C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#91424B"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FocusVisualStyle" Value="{StaticResource KeyboardFocus}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#52212A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SessionItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBd" Background="#14141C"
                                BorderBrush="#28FFFFFF" BorderThickness="1"
                                CornerRadius="10" Padding="14,12">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBd" Property="Background" Value="#20202A"/>
                                <Setter TargetName="ItemBd" Property="BorderBrush" Value="#50FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBd" Property="Background" Value="#20202A"/>
                                <Setter TargetName="ItemBd" Property="BorderBrush" Value="#70FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#0E1016" CornerRadius="18"
            BorderBrush="#40FFFFFF" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="40" ShadowDepth="0" Color="#000000" Opacity="0.7"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ── HEADER ── -->
            <Border Grid.Row="0" Background="#A0060810" CornerRadius="18,18,0,0"
                    BorderBrush="#28FFFFFF" BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="20,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left: icon + title -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="#28FFFFFF" CornerRadius="8" Width="32" Height="32" Margin="0,0,12,0">
                            <TextBlock Text="&#xE8BD;" FontFamily="Segoe MDL2 Assets" FontSize="15"
                                       Foreground="White"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Interview Sessions" Foreground="White"
                                       FontSize="14" FontWeight="Bold" FontFamily="Segoe UI"/>
                            <TextBlock x:Name="SubtitleLabel" Text="Loading…"
                                       Foreground="#9A9AA0" FontSize="10" FontFamily="Segoe UI"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Right: close button -->
                    <Button Grid.Column="2" Content="✕" Click="CloseBtn_Click"
                            Background="Transparent" BorderThickness="0"
                            Foreground="#C4C4C8" FontSize="16" Cursor="Hand" Padding="8,4"/>
                </Grid>
            </Border>

            <!-- ── BODY ── -->
            <Grid Grid.Row="1" Margin="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- LEFT: Sessions list -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Sessions list -->
                    <Border Grid.Row="0" Background="#101017" CornerRadius="12"
                            BorderBrush="#28FFFFFF" BorderThickness="1">
                        <Grid>
                            <!-- Empty state -->
                            <StackPanel x:Name="EmptyState" VerticalAlignment="Center"
                                        HorizontalAlignment="Center" Visibility="Collapsed">
                                <TextBlock Text="📭" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                                <TextBlock Text="No sessions yet" Foreground="#9A9AA0"
                                           FontSize="13" FontFamily="Segoe UI"
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="Sessions are saved automatically" Foreground="#8A8A90"
                                           FontSize="11" FontFamily="Segoe UI"
                                           HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>

                            <!-- List -->
                            <ListBox x:Name="SessionsList"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     ItemContainerStyle="{StaticResource SessionItemStyle}"
                                     SelectionChanged="SessionsList_SelectionChanged"
                                     Padding="10">
                                <ListBox.Template>
                                    <ControlTemplate TargetType="ListBox">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </ControlTemplate>
                                </ListBox.Template>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="3"/>
                                                <ColumnDefinition Width="10"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <!-- Accent bar -->
                                            <Border Grid.Column="0" Background="#FFFFFF" CornerRadius="2" VerticalAlignment="Stretch"/>
                                            <!-- Content -->
                                            <StackPanel Grid.Column="2" Margin="0,2,0,2">
                                                <TextBlock Text="{Binding DisplayTitle}"
                                                           Foreground="White" FontSize="13"
                                                           FontWeight="Bold" FontFamily="Segoe UI"
                                                           Margin="0,0,0,4"/>
                                                <TextBlock Text="{Binding DisplayDate}"
                                                           Foreground="#9A9AA0" FontSize="11"
                                                           FontFamily="Segoe UI" Margin="0,0,0,3"/>
                                                <StackPanel Orientation="Horizontal">
                                                    <Border Background="#18FFFFFF" CornerRadius="4" Padding="6,2" Margin="0,0,6,0">
                                                        <TextBlock Text="{Binding DisplayStats}"
                                                                   Foreground="#FFFFFF" FontSize="10"
                                                                   FontWeight="SemiBold" FontFamily="Segoe UI"/>
                                                    </Border>
                                                    <TextBlock Text="{Binding DisplayModel}"
                                                               Foreground="#8A8A90" FontSize="10"
                                                               FontFamily="Segoe UI" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>

                    <!-- Delete button -->
                    <Button x:Name="DeleteBtn" Grid.Row="1"
                            Content="🗑  Delete Session"
                            Style="{StaticResource DangerBtn}"
                            Click="DeleteBtn_Click"
                            Padding="14,8" Margin="0,10,0,0"
                            HorizontalAlignment="Stretch"
                            Visibility="Collapsed"/>
                </Grid>

                <!-- Splitter -->
                <Border Grid.Column="1" Width="1" Background="#18FFFFFF" VerticalAlignment="Stretch" Margin="0,4"/>

                <!-- RIGHT: Transcript viewer -->
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Session detail header -->
                    <Border Grid.Row="0" x:Name="DetailHeader"
                            Background="#14141C" CornerRadius="12"
                            BorderBrush="#28FFFFFF" BorderThickness="1"
                            Padding="16,10" Margin="0,0,0,10"
                            Visibility="Collapsed">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock x:Name="DetailTitle" Text="" Foreground="White"
                                           FontSize="14" FontWeight="Bold" FontFamily="Segoe UI"/>
                                <TextBlock x:Name="DetailMeta" Text="" Foreground="#9A9AA0"
                                           FontSize="11" FontFamily="Segoe UI" Margin="0,3,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button x:Name="CopyTranscriptBtn"
                                        Content="📋 Copy Text"
                                        Style="{StaticResource GhostBtn}"
                                        Click="CopyTranscriptBtn_Click"
                                        Padding="12,6" Margin="0,0,8,0"/>
                                <Button x:Name="ExportBtn"
                                        Content="⬇ Export .txt"
                                        Style="{StaticResource GhostBtn}"
                                        Click="ExportBtn_Click"
                                        Padding="12,6"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Transcript -->
                    <Border Grid.Row="1" Background="#101017" CornerRadius="12"
                            BorderBrush="#18FFFFFF" BorderThickness="1">
                        <!-- Grid wrapper — Border can only hold one child -->
                        <Grid>
                            <!-- Placeholder when nothing selected -->
                            <StackPanel x:Name="NoSelectionState"
                                        VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="←" Foreground="#9A9AA0" FontSize="32"
                                           HorizontalAlignment="Center" Margin="0,0,0,12"/>
                                <TextBlock Text="Select a session to view its transcript"
                                           Foreground="#9A9AA0" FontSize="13"
                                           FontFamily="Segoe UI" HorizontalAlignment="Center"/>
                            </StackPanel>

                            <!-- Transcript scroll view -->
                            <ScrollViewer x:Name="TranscriptScroll"
                                          VerticalScrollBarVisibility="Auto"
                                          Padding="20,16"
                                          Visibility="Collapsed">
                                <StackPanel x:Name="TranscriptPanel"/>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
